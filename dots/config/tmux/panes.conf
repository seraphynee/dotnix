# === PANES ===

# == pane style 1 ==
# set -g pane-active-border-style "fg=$_tmux_blue"
# set -g message-style "fg=$_tmux_grey_l,bg=$_tmux_grey_d"
# set -g pane-border-style "fg=white"
# set -g display-panes-colour "black"
# set -g display-panes-active-colour "white"
#
# set -g pane-border-status "off"
# set -g pane-border-format "[#[fg=white]#{?pane_active,#[bold],} :#P: #T #[fg=default,nobold]]"
# == pane style 1 ==

# == pane style 2 ==
set -g pane-border-status off        # tidak tampilkan title
set -g pane-border-format ""         # format dikosongkan agar benar-benar tidak muncul

set -g pane-border-style "fg=white"  # warna pane normal
set -g pane-active-border-style "fg=green"  # active pane jadi hijau

# set -g message-style "fg=$_tmux_grey_l,bg=$_tmux_grey_d"
set -g display-panes-colour "black"
set -g display-panes-active-colour "white"
# == pane style 2 ==

# == pane style 3 ==
# set -g pane-border-status top
# set -g pane-border-style "fg=white"
# set -g pane-active-border-style "fg=$_tmux_green"
#
# set -g pane-border-format " #[fg=green,bold]#{?@mytitle,#{@mytitle},pane #P}#[default] "
#
# bind u command-prompt -p "Enter new pane title:" "set-option -p @mytitle '%%'"
# == pane style 3 ==

# Auto hide pane-border-status line when only one pane is displayed (see tmux/tmux#566)
set-hook -g 'after-new-session'  'run-shell -b "if [ \#{window_panes} -eq 1 ]; then tmux set pane-border-status off; fi"'
set-hook -g 'after-new-window'   'run-shell -b "if [ \#{window_panes} -eq 1 ]; then tmux set pane-border-status off; fi"'
set-hook -g 'after-kill-pane'    'run-shell -b "if [ \#{window_panes} -eq 1 ]; then tmux set pane-border-status off; fi"'
set-hook -g 'pane-exited'        'run-shell -b "if [ \#{window_panes} -eq 1 ]; then tmux set pane-border-status off; fi"'
set-hook -g 'after-split-window' 'run-shell -b "if [ \#{window_panes} -gt 1 ]; then tmux set pane-border-status top; fi"'
