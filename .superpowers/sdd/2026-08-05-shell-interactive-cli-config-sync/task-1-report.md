# Task 1 implementation report

## Commit

- Implementation commit: `66d68bd` (`feat(shell): sync fish interactive cli config`)

## Files changed

- `modules/shell/fish.nix`
- `dots/config/fish/conf.d/abbreviations.fish`
- `dots/config/fish/conf.d/atuin.fish`
- `dots/config/fish/conf.d/bat.fish`
- `dots/config/fish/conf.d/chezmoi.fish`
- `dots/config/fish/conf.d/common_aliases.fish`
- `dots/config/fish/conf.d/common_functions.fish`
- `dots/config/fish/conf.d/herdr.fish`
- `dots/config/fish/conf.d/jujutsu.fish`
- `dots/config/fish/conf.d/yazi.fish`

The platform-owned `dots/config/fish/env.d/000-xdg.fish` and `030-secrets.fish` were read but not edited. VCS modules were not edited.

## Decisions

- Rendered Darwin/Linux clipboard command selection in Nix using `pkgs.stdenv.hostPlatform.isDarwin` and `.isLinux`; `cpwd` checks the selected command and returns non-zero when unavailable.
- Ported the source abbreviation set and retained the F2 placeholder widget, with XDG environment variables replacing source-only home paths. Git abbreviations remain command aliases only; Git/JJ program settings remain in their owning modules.
- Added explicit `wq`, `wcd`, and `wacd` wrappers that run `command jj` first, validate the returned directory, and change the parent Fish shell only after success.
- Added guarded Atuin, Bat, Chez Moi, Herdr, Jujutsu, and Yazi integrations. `nvdot` uses `$XDG_DATA_HOME/chezmoi`, whose value is generated by the platform env module.
- Left XDG and sops secret loading to Fish's generated `env.d` files. The bridge remains silent when optional files are absent.
- Did not add packages to shell files; package ownership stays in Nix modules.

## Verification

- `fish --no-config -n` over every `dots/config/fish/**/*.fish`: exit 0; 12 files checked; empty output.
- `rg -n 'bind .*f2|BAT_THEME|cpwd|function y|function wq|nvdot|XDG_DATA_HOME|CONTEXT7_MCP_API_KEY' dots/config/fish modules/shell/fish.nix`: exit 0; every required contract present.
- `rg -n -i 'onepasswordRead|\\.shell_env|\\.xdg\\.|\\{\\{' dots/config/fish`: exit 1 as expected because no forbidden expressions matched; reported as empty output.
- `nix-instantiate --parse modules/shell/fish.nix`: exit 0; empty output.
- `git diff --check`: exit 0; empty output.
- Pre-commit `nix fmt`: passed (`formatted 3 files (3 changed) in 115ms`), then commit completed.

## Concerns

- A full Home Manager/NixOS evaluation was not run; the requested Fish syntax and structural checks plus Nix parsing passed.
- The first commit-hook attempt was blocked by the sandbox's read-only Nix fetcher cache. The approved retry passed. The hook formatter also touched two unrelated files; those formatter-only working-tree changes were restored and are not part of the implementation commit.

## Fix round 1

### Findings addressed

- Guarded the Herdr hook scan with `type -q herdr`, safely collected matching hooks with `find`, and retained first-readable-hook sourcing.
- Removed the redundant F2 binding from `modules/shell/fish.nix`; the abbreviations file remains the sole owner of the helper and insert-mode binding.

### Verification

- `fish --no-config -n` over every `dots/config/fish/**/*.fish`: exit 0; 12 files checked; empty output.
- `rg -n 'type -q herdr' dots/config/fish/conf.d/herdr.fish`: exit 0; command guard present.
- `rg -n 'bind .*f2' dots/config/fish modules/shell/fish.nix | wc -l`: output `1`; the sole binding is in `abbreviations.fish`.
- `rg -n -i 'onepasswordRead|\\.shell_env|\\.xdg\\.|\\{\\{' dots/config/fish`: exit 1 as expected because no forbidden expressions matched; empty output.
- `git diff --check`: exit 0; empty output.

### Fix commit

- Fix commit: `e14bf6b` (`fix(shell): address fish review findings`).
